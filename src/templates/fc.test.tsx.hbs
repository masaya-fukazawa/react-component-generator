import React from 'react'
import { {{~name~}} } from './{{name}}'
import {expectSnapshot} from '@tests/expects'

describe('{{directory}}/{{name}}', () => {
  test('snapshot', () => expectSnapshot(<{{name}}/>))
})
